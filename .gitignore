.venv/
.cursor/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ipynb_checkpoints/

# --- Large / local-only files (folder layout kept via .gitkeep) ---

# ArcFace weights (copy locally; see README)
biometric_authorization/results/*.pth
biometric_authorization/results/*.pt

# MediaPipe bundle (Dockerfile download or manual)
biometric_authorization/models/*.task

# SpeechBrain ECAPA cache (Dockerfile / runtime)
biometric_authorization/models/speechbrain_ecapa/

# Lokalne zbiory, SQLite, korpusy — zawartość data/ poza repozytorium (wyjątek: README.md)
biometric_authorization/data/*
!biometric_authorization/data/README.md
